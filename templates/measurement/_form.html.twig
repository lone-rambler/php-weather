{{ form_start(form) }}
    {{ form_widget(form) }}
    {% if is_granted('ROLE_MEASUREMENT_NEW') %}
    <button class="btn">{{ button_label|default('Save') }}</button>
    {% endif %}
{{ form_end(form) }}

